[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "web-scrapper"
version = "0.1.0"
description = "Web page scraping logic"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "beautifulsoup4",
    "markdownify",
    "openai",
    "playwright",
    "qdrant-client",
    "crawler-shared",
    "platform-common",
    "playwright-stealth>=2.0.3",
    "fastembed>=0.8.0",
]

[tool.uv.sources]
crawler-shared = { workspace = true }
platform-common = { path = "../../../shared-libs/platform-common", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/web_scrapper"]
