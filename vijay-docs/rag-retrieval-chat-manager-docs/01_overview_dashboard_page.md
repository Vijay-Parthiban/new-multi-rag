# Overview / Dashboard Page

## Route
`/` (default route)

## Component
`AppLayout.tsx` (Sidebar mapping `HomePage` component representation, likely empty or metadata driven as `overview`)

## Features
Displays top-level aggregated statistics covering active pipelines, active models, recent chat streams, and evaluation statuses. Serves as the landing page for the Retrieval Ops portal.